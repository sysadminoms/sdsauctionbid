# Port
server.port=5002

## Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
spring.datasource.url = jdbc:mysql://sdsauction.cithtssobjxc.ap-south-1.rds.amazonaws.com/sds_auction
spring.datasource.username = admin
spring.datasource.password = 65>ak`8z>mmB8gYf

## App Properties
app.jwtSecret= oms_auction
app.jwtExpirationInMs = 900000

## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect
# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = none

# HikariCP settings
# spring.datasource.hikari.*

spring.datasource.hikari.connection-timeout=60000
spring.datasource.hikari.maximum-pool-size=5

# logging
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n
logging.level.org.hibernate.SQL=debug
#logging.level.org.hibernate.type.descriptor.sql=trace
logging.level.=error

## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=200MB
# Max Request Size
spring.servlet.multipart.max-request-size=215MB

## File Storage Properties
# All files uploaded through the REST API will be stored in this directory
file.upload-dir=E:/Uploaded_Files
#file.upload-dir=//192.168.0.241/test

# AWS Credentials
aws.access.key.id = AKIA4IWD66EEAE3GMLMG
aws.access.key.secret = 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
aws.region = ap-northeast-2
aws.s3.bucket = sdsauction

#Email Smtp Configuration
mail.smtp.port= 465
spring.mail.protocol=smtp
spring.mail.host=mail.jobastra.in
spring.mail.username=recruiter@jobastra.in
spring.mail.password=5$zcBT!j6CPO
spring.mail.properties.mail.smtp.auth = true
spring.mail.properties.mail.smtp.starttls.enable = true
spring.mail.properties.mail.smtp.ssl.enable = true

